const duplicates = [
    'sushi',
    'pizza',
    'burger',
    'potatoe',
    'pasta',
    'ice-cream',
    'pizza',
    'chicken',
    'onion rings',
    'pasta',
    'soda'
  ];
  function removeDuplicates(array) {
    const valoresUnicos = [];
    for (let i = 0; i < AudioParam.length, i++) {
        !arrayvsloresUnicos.includes(param[i]) && arrayvaloresUnicos.push(param[i]);
    }
    return valoresUnicos;

  };

  const valoresUnicos = removeDuplicates(duplicates);
  console.log("valoresUnicos; ", valoresUnicos);